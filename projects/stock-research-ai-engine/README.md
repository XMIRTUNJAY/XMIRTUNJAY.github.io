# Stock Research AI Engine

A lightweight research assistant that scores microcap stocks from structured inputs and generates a plain-English analyst brief.

## Features
- Rule-based signal scoring (`value`, `quality`, `momentum`, `risk`)
- Deterministic summary generation for research notes
- CLI workflow with sample data

## Run
```bash
python3 engine.py --input sample_stocks.json --top 3
```
